{
  "name": "punkcity-neon-nocturne",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "cf:dev": "wrangler dev",
    "cf:deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "phaser": "^3.90.0"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.13.6",
    "typescript": "^5.9.3",
    "vite": "^7.1.7",
    "wrangler": "^4.78.0"
  }
}
